@{
    ViewData["Title"] = "Admin Dashboard";
}
@model List<Reservation>

<h2>Admin Dashboard - Reservations</h2>

<table class="table table-striped table-bordered mt-4">
    <thead class="table-dark">
        <tr>
            <th scope="col">#</th>
            <th scope="col">User Details</th>
            <th scope="col">Number of Accommodations</th>
            <th scope="col">Total Price</th>
            <th scope="col">Status</th>
            <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            for (int i = 0; i < Model.Count; i++)
            {
                var reservation = Model[i];
                <tr style="@(reservation.Status == ReservationStatus.Cancelled ? "opacity:0.6" : "")">
                    <th scope="row">@(i + 1)</th>
                    <td>@reservation.User.FirstName @reservation.User.LastName - @reservation.User.Email</td>
                    <td>@reservation.AccommodationInReservations.Count()</td>
                    <td>@reservation.TotalPrice.ToString("0") €</td>
                    <td>
                        @if (reservation.Status == ReservationStatus.Confirmed)
                        {
                            <span class="badge bg-success">Confirmed</span>
                        }
                        else
                        {
                            <span class="badge bg-danger">Cancelled</span>
                        }
                    </td>
                    <td>
                        <a class="btn btn-outline-secondary btn-sm" asp-action="Details" asp-route-id="@reservation.Id">Details</a>
                        <a class="btn btn-outline-primary btn-sm" asp-action="CreateInvoice" asp-route-id="@reservation.Id">Create Invoice</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="6" class="text-center">No reservations found.</td>
            </tr>
        }
    </tbody>
</table>